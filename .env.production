# URL of the deployed Cloudflare Worker (worker/) that proxies Claude API calls.
# Not a secret - it's just the public endpoint the frontend calls.
# See worker/README.md for how to deploy the worker and get this URL.
# Leave empty (or unset) to disable AI auto-analysis and use manual entry only.
VITE_ANALYZE_API_URL=
